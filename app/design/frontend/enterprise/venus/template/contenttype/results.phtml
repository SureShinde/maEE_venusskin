<?php
$collection = $this->getCollection();
$link = $this->getLink();
?>
<div class="patients-section patients-results">
    <div class="patients-section__container">
        <div class="patients-results__row">
            <div class="patients-results__col patients-results__col--content">
                <?php echo $this->getLayout()->createBlock('cms/block')->setBlockId('patients_results')->toHtml(); ?>
            </div>
            <div class="patients-results__col  patients-results__col--slider">
                <div class="patients-results__slider">
                    <?php if ($collection->getSize()): ?>
                        <div class="results">
                            <div class="results__container owl-carousel owl-theme">
                                <?php foreach ($collection as $content): ?>
                                    <div class="results__item">
                                        <div class="results__image">
                                            <?php echo trim($content->render('result_image')); ?>
                                        </div>
                                        <div class="results__author">
                                            <div class="results__condition">
                                                <?php echo $content->render('result_condition', ['html_tag' => false]); ?>
                                            </div>
                                            <div class="results__description">
                                                <?php echo $content->render('result_description', ['html_tag' => false]); ?>
                                            </div>
                                            <div class="results__clinic">
                                                <?php echo $content->render('result_clinic', ['html_tag' => false]); ?>
                                            </div>
                                        </div>
                                    </div>
                                <?php endforeach; ?>
                            </div>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        </div>
    </div>
</div>
<script type="text/javascript">
    jQuery(function($) {
        $(".results__container").owlCarousel({
            items: 1,
            loop: false,
            nav: true,
            autoplay: true,
            autoplayTimeout: 4000,
            autoplayHoverPause: true
        });
        $('.results__image a').colorbox();
    });
</script>
